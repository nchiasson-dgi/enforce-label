{
  "name": "enforce-label",
  "version": "1.0.0",
  "description": "Action for enforcing labels on a pull-request.",
  "main": "src/index.ts",
  "scripts": {
    "test": "eslint src/index.ts",
    "build": "tsc",
    "package": "npm run build && ncc build --source-map"
  },
  "author": "discoverygarden",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1",
    "eslint": "^8.40.0",
    "typescript": "^5.0.4"
  }
}
